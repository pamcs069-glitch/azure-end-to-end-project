{
  "name": "ds_bronze_payments_sink",
  "properties": {
    "linkedServiceName": {
      "referenceName": "ls_adls_bronze",
      "type": "LinkedServiceReference"
    },
    "parameters": {
      "p_ingestion_date": {
        "type": "string"
      },
      "p_page_number": {
        "type": "int"
      }
    },
    "type": "Json",
    "typeProperties": {
      "location": {
        "type": "AzureBlobFSLocation",
        "fileSystem": "bronze",
        "folderPath": {
          "value": "@concat('api/payments/raw/ingestion_date=', dataset().p_ingestion_date)",
          "type": "Expression"
        },
        "fileName": {
          "value": "@concat('page_', if(less(dataset().p_page_number, 10), concat('00', string(dataset().p_page_number)), if(less(dataset().p_page_number, 100), concat('0', string(dataset().p_page_number)), string(dataset().p_page_number))), '.json')",
          "type": "Expression"
        }
      }
    }
  }
}
